[package]
name = "bitvmx-job-dispatcher-types"
version = "0.1.0"
edition = "2021"

[dependencies]

serde = { version = "1.0.219", features = ["derive"] }
bitvmx-cpu-definitions = { path = "../../BitVMX-CPU/definitions", optional = true }
emulator = { path = "../../BitVMX-CPU/emulator" }                                   # Remove in production?
serde_json = "1.0.64"
thiserror = "2.0.11"
hex = "0.4"
bitvmx-job-dispatcher = { path = "../dispatcher" }

[features]
prover = []
emulator = ["bitvmx-cpu-definitions"]
